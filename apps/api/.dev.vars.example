# Local development-only variables for wrangler dev
APP_VERSION="1.0.0"
ENVIRONMENT="dev"
# Comma-separated strict allowlist. Wildcard '*' is intentionally rejected.
ALLOWED_ORIGINS="http://127.0.0.1:3000,http://localhost:3000"
OPENAI_API_KEY=""
# Keep off by default. Enables deprecated Worker mutation endpoints temporarily.
ALLOW_LEGACY_MUTATIONS="0"
